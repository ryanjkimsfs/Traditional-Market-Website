# Copy to .env (same folder as server.py) and fill in your key.
# Without it the site still works — the offline recommender answers instead.

XAI_API_KEY=xai-your-key-here

# Optional. Any xAI chat model id works; grok-4 is the default.
XAI_MODEL=grok-4
